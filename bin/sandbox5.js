#!/usr/bin/env node

// Use the isItAPalindrome() function to return Boolean if the String is a palindrome.

const start = () => {

    let possiblePalindrome = "Zerorez";

    console.log(isItAPalindrome(possiblePalindrome));
};

const isItAPalindrome = (String) => {
    // Use this space to solve the problem
};

start();
