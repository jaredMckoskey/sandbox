#!/usr/bin/env node --experimental-modules

// Use the isItAnAnagram() function to return Boolean if the two Strings are an anagram.

const start = () => {

    let string1 = "Listen";
    let string2 = "Silent";

    console.log(isItAnAnagram(string1, string2));
};

const isItAnAnagram = (String1, String2) => {
    // Use this space to solve the problem
};

start();
